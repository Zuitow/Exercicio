import { StyleSheet } from "react-native";

export const estilos = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
    Inputs: {
     width: 325,
     height: 40,
     fontSize: 20,
     marginTop: 15,
     borderBottomWidth: 5,
     borderColor: 'black',
     borderRadius: 8,
    }
  });
  